apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana
  name: grafana
  namespace: monitoring
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http-grafana
  selector:
    app: grafana
  type: LoadBalancer
  sessionAffinity: None
