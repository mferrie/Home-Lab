apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: core-claim0
  name: core-claim0
  namespace: searxng
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
