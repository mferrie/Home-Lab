apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: core-data
  name: core-data
  namespace: searxng
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
