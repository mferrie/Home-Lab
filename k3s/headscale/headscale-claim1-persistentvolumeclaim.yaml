apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: headscale-claim1
  name: headscale-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
